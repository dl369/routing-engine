# routing engine
Routing engine in Go designed to compute efficient point-to-point paths across map and transit networks. 
Focused on high performance with in memory data structures, concurrent api polling and performant algorithms.
